//ESCOLHA E CASO
let valor = parseInt(prompt(`digite valor`)) 
switch (valor) {
    case 1:
        console.log(`um`)
        break;

    default:
        console.log(`valor não reconhecido`)
        break;
}